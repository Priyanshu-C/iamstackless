Product images for the watches drawer.

Name each file after the item id in lib/collections/watches.ts, e.g. seiko-skx007.webp
for { id: "seiko-skx007" }. The data-integrity test fails the suite if an
item points at a file that is not here.

# The Case — /collections
# Both must be set, or the case stays locked for everyone (it fails closed).

# The shared password typed into the lock screen.
COLLECTIONS_PASSWORD=

# Signs the unlock cookie so it cannot be forged. Any long random string.
# Generate one with:  openssl rand -base64 32
COLLECTIONS_SECRET=
